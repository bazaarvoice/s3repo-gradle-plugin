buildscript {
    repositories {
        mavenLocal()
    }

    dependencies {
        classpath 'com.bazaarvoice.gradle:s3repo-gradle-plugin:1.0'

    }
}

apply plugin: 'com.bazaarvoice.s3repo'

s3repo {
    allowCreateRepo = true
    s3RepositoryUrl = "s3://nexus-private-artifacts/airpal/yum-repo/"
    inputs = files("build.gradle")
}